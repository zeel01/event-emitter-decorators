import { emitter, emit, on, emits, EventEmitter } from "./src/EventEmitter.js";

export default emitter;

export { emitter, emit, on, emits, EventEmitter };

export {
	emitter as Emitter,
	emit as Emit,
	on as On,
	emits as Emits
}